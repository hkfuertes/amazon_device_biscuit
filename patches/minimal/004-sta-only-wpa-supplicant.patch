--- a/external/wpa_supplicant_8/wpa_supplicant/android.config	2026-09-14 17:54:21.762617642 +0200
+++ b/external/wpa_supplicant_8/wpa_supplicant/android.config	2026-09-14 17:54:21.766617666 +0200
@@ -140,14 +140,15 @@
 #CONFIG_EAP_TNC=y
 
 # Wi-Fi Protected Setup (WPS)
-CONFIG_WPS=y
+# ponytail: Biscuit minimal is STA-only; re-enable WPS after verified support.
+# CONFIG_WPS=y
 # Enable WPS external registrar functionality
-CONFIG_WPS_ER=y
+# CONFIG_WPS_ER=y
 # Disable credentials for an open network by default when acting as a WPS
 # registrar.
 #CONFIG_WPS_REG_DISABLE_OPEN=y
 # Enable WPS support with NFC config method
-CONFIG_WPS_NFC=y
+# CONFIG_WPS_NFC=y
 
 # EAP-IKEv2
 #CONFIG_EAP_IKEV2=y
@@ -434,10 +435,11 @@
 # This can be used to enable functionality to improve interworking with
 # external networks (GAS/ANQP to learn more about the networks and network
 # selection based on available credentials).
-CONFIG_INTERWORKING=y
+# ponytail: Biscuit minimal is STA-only; re-enable interworking after verified support.
+# CONFIG_INTERWORKING=y
 
 # Hotspot 2.0
-CONFIG_HS20=y
+# CONFIG_HS20=y
 
 # Disable roaming in wpa_supplicant
 CONFIG_NO_ROAMING=y
@@ -447,12 +449,14 @@
 # should be noted that this is mainly aimed at simple cases like
 # WPA2-Personal while more complex configurations like WPA2-Enterprise with an
 # external RADIUS server can be supported with hostapd.
-CONFIG_AP=y
+# ponytail: Biscuit minimal never acts as an AP.
+# CONFIG_AP=y
 
 # P2P (Wi-Fi Direct)
 # This can be used to enable P2P support in wpa_supplicant. See README-P2P for
 # more information on P2P operations.
-CONFIG_P2P=y
+# ponytail: Biscuit minimal uses STA only.
+# CONFIG_P2P=y
 
 # Enable TDLS support
 CONFIG_TDLS=y
@@ -460,7 +464,7 @@
 # Wi-Fi Direct
 # This can be used to enable Wi-Fi Direct extensions for P2P using an external
 # program to control the additional information exchanges in the messages.
-CONFIG_WIFI_DISPLAY=y
+# CONFIG_WIFI_DISPLAY=y
 
 # Autoscan
 # This can be used to enable automatic scan support in wpa_supplicant.
--- a/external/wpa_supplicant_8/wpa_supplicant/Android.mk	2026-09-14 17:54:21.762617642 +0200
+++ b/external/wpa_supplicant_8/wpa_supplicant/Android.mk	2026-09-14 17:54:21.766617666 +0200
@@ -26,7 +26,8 @@
 L_CFLAGS += -Wno-unused-parameter
 
 # Set Android extended P2P functionality
-L_CFLAGS += -DANDROID_P2P
+# ponytail: Biscuit minimal uses MTK STA only; Android P2P hooks are not needed.
+# L_CFLAGS += -DANDROID_P2P
 
 ifeq ($(BOARD_WPA_SUPPLICANT_PRIVATE_LIB),)
 L_CFLAGS += -DANDROID_LIB_STUB
--- a/external/wpa_supplicant_8/wpa_supplicant/ctrl_iface.c	2026-09-14 18:53:39.487971859 +0200
+++ b/external/wpa_supplicant_8/wpa_supplicant/ctrl_iface.c	2026-09-14 18:53:39.487971859 +0200
@@ -6989,6 +6989,7 @@
 
 	ret = wpa_drv_driver_cmd(wpa_s, cmd, buf, buflen);
 	if (ret == 0) {
+#ifdef CONFIG_P2P
 		if (os_strncasecmp(cmd, "COUNTRY", 7) == 0) {
 			struct p2p_data *p2p = wpa_s->global->p2p;
 			if (p2p) {
@@ -6999,6 +7000,7 @@
 				p2p_set_country(p2p, country);
 			}
 		}
+#endif
 		ret = os_snprintf(buf, buflen, "%s\n", "OK");
 		if (os_snprintf_error(buflen, ret))
 			ret = -1;
