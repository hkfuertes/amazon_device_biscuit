# CM14.1 boot diagnostics

## Scope and safety

Session: 2026-09-08, host local time (CEST). Device: `G090L91073533XR7`.
The user authorized continued autonomous diagnosis after the quick MTK-header test.
Only `boot_a_x` is being installed, through verified TWRP 3.2.3-0 or confirmed
amonet hacked fastboot on slot `_a`. Every successful installation has a fresh
host-side boot backup and exact image-extent readback. Experimental TWRP installs
are read back before testing; a known capture-kernel reinstallation through fastboot
is read back from that kernel after boot, deliberately avoiding intervening TWRP.
No system updates, wipes, protected-partition changes, or full ROM/OTA builds were performed.
An isolated kernel-only capture build is described below. The stopped
`cm14.1-biscuit-build` container remains stopped.

## Results

| Test | Result | Evidence |
| --- | --- | --- |
| Compiled Fire OS 6.5.7.1 kernel with one MTK header, previous CM14 runtime-fstab ramdisk | No observed Android ADB; subsequently reached preloader again and was recovered to TWRP | Installed/readback SHA-256 `5cd16e1f41d74e9e68cff0822a6b084953f9a6d91c09fc7844a551424f19744e`; recovery helper caught preloader at 00:22:30 |
| CM12 kernel with minimal ARM32 init/adbd ramdisk | Positive control: authenticated ADB, uptime 225 seconds, no block filesystems mounted | Installed/readback SHA-256 `01ac44805e20299a772ce3f6540f9d5051d4f81091d0ed5b202df8e348075cb8`; `cm12-control.identity.log` and `cm12-control.dmesg.log` |
| Compiled Fire OS 6.5.7.1 kernel with MTK header and the identical minimal ramdisk | No ADB; preloader-to-preloader period 64.091 seconds; recovered to TWRP | SHA-256 `9e9b5fb460e3f5a386b20e426dc9ecc9a07144256859169d8e511d3c570e15e1`; `fireos6-mtk.usb.log` |
| Stock Fire OS 6.5.7.4 kernel with MTK header and the same minimal ramdisk | No ADB; preloader-to-preloader period 64.088 seconds; recovered to TWRP | SHA-256 `3dc1ae06756817affd9adb4b03b162076ef05f68642d17352e429b76fb9ed1a9`; `fireos6-stock.usb.log` |
| Stock Fire OS 6.5.7.4, removing only its MTK header from the preceding probe | No ADB; preloader recurred; one-shot recovery guard returned TWRP at 00:55:06 | SHA-256 `6a40bebab18c165c9f6b1467d4536f718d79ef3dbbe22103a2a9acf9543375f3`; `fireos6-stock-raw.observe.log` and `fireos6-stock-raw.recovery-helper.log` |
| Same stock-raw probe, adding only `maxcpus=1` to its header command line | No probe ADB; guard caught preloader at 01:07:48 and returned TWRP at 01:08:08 | SHA-256 `e1fb91f16dbb7cbddf50916401011690665156d9657f04dd5ef82de87471269c`; `fireos6-stock-raw-up.observe.log` and `fireos6-stock-raw-up.recovery-helper.log` |

The original header experiment was not a strict header-only comparison with the
previously installed image: that image used the stock 6.5.7.4 kernel, already
MTK-wrapped. The raw compiled 6.5.7.1 kernel was verified unchanged before wrapping.
A reboot and missing ADB do not identify a panic or a specific failing stage.
The failure persists without system/framework startup, with either Fire OS 6 kernel,
and with or without the stock kernel's MTK wrapper. This narrows investigation to
kernel/handoff or kernel-specific interaction with the diagnostic ramdisk; it does
not prove a particular kernel defect.

## ADB-only probe

Generator (offline, no device operations):

```sh
python3 scripts/diagnostics/make-adb-boot-probe.py
```

Bounded read-only observer (launch detached before a separately authorized reboot):

```sh
nohup python3 scripts/diagnostics/observe-biscuit-boot.py '<test-label>' \
  </dev/null >/tmp/biscuit-boot-observer.log 2>&1 &
```

The observer does not flash, reboot, or recover the device. For the last test, a
separate one-shot host guard waited 45 seconds, confirmed absence of the target's
ADB transport and of another helper, then launched only the authorized root-owned
`boot-recovery.sh`. This caught the next preloader cycle without another blind boot.

Artifacts and logs: `workspace/diagnostics/cm14-adb-boot-probe/` (git-ignored).
The generator pins input hashes and verifies CPIO round trips, boot IDs, unchanged
kernel payload/cmdline/other components, ZIP integrity, and boot-only installer scope.
The explicit `-raw` variant removes exactly one validated 512-byte MTK header; it
preserves every byte of the underlying kernel/DTB payload. The `-raw-up` variant
changes only the header command line by appending `maxcpus=1`; its failed test does
not establish whether that command line reached the ARM32 kernel.
It reuses existing static ARM32 CM12 recovery binaries without compiling.
It does **not** launch the recovery UI or mount system, data, cache, or other blocks.
Only init, ueventd, and adbd run; the recovery multicall binary supplies the shell.
The public host key is embedded in `/adb_keys`; `ro.adb.secure=1` remains enabled.
The probes preserve their respective kernel addresses and architecture-specific
`bootopt`; therefore the comparison is kernel plus its required boot metadata, not
an identical-header cross-architecture comparison.

The positive control reports Linux 3.18.19 **aarch64** with ARM32 userspace.
The Fire OS 6 kernel is also Linux 3.18.19 but **ARM32**. Do not describe this as a
3.10-versus-3.18 comparison. The control's live command line has no `rdinit` override.
Its init/adbd startup and USB configuration are visible at approximately 2 seconds
in kernel time. Its mount table contains only rootfs, tmpfs, devpts, proc, sysfs,
selinuxfs, and FunctionFS.

### Host authentication pitfall

The initial control observer treated a failed `adb get-state` as absence, hiding
an `unauthorized` transport. Explicit `adb devices -l` and USB descriptors revealed
that the probe was running (`18d1:d001`, product `BootProbe`).
The public key embedded in the probe matched the current user's private-key-derived
public key, but the existing host ADB server was running as root. After confirming
ordinary-user read/write access to the USB node, `adb kill-server` and
`adb start-server` restarted it as the user; authentication succeeded without
changing the image, disabling authentication, deleting keys, or using sudo.
Subsequent observers distinguish unauthorized/offline/recovery/device states.

### Boot ID detail

The pinned CM12 boot image's SHA-1 ID covers kernel, ramdisk, and second stage.
The prior diagnostic image also includes the empty DT size. The generator detects
and preserves each verified format instead of assuming one universal variant.
This was caught by an assertion before a probe ZIP was generated; it is not a
proven cause of any device boot failure.

## Installed amonet provenance (read-only)

The 13,888-byte installed `boot.payload` matches
`/opt/amonet-biscuit-v1.1.0/amonet/bin/boot.payload` exactly:
SHA-256 `794e039afa1f168601ebd73e28faaf1cef83e6dbeddeb862f75e4dcdbe4eaf53`.
The read-only copy is `installed-boot-a-payload.readonly.bin` in the diagnostic folder.
The payload includes both ARM32/ARM64 detection messages. The local source at
`/tmp/amonet-biscuit`, commit `fff316c`, selects the mode from the third `bootopt`
field and writes LK's mode flag. The installed bytes match the packaged payload;
source-to-binary equivalence for that local checkout has not been established.
The actual ARM32 branch execution and handoff registers are not observed.
No exploit or bootloader write was performed.

TWRP remaps `/dev/block/platform/soc/by-name/boot_a` to `boot_a_x` (`mmcblk0p10`).
An initial size guard rejected that alias before attempting a payload read.
Kernel sysfs identifies the real exploit partition as `mmcblk0p17`, `PARTNAME=boot_a`,
size 115,343,360 bytes. Only the packaged payload extent at offset 114,281,984
(`223207 * 512`) was read from it after validating identity and bounds.
Do not use the physical exploit partition as a ROM flash target.

## Read-only RAM-console capture (in progress)

`fireos6-stock-raw.last_kmsg` contains previous TWRP activity: `recovery` process
records and ARM64 addresses, not a demonstrated panic from the failed ARM32 probe.
The embedded configs of both Fire OS 6 kernels enable a 64-KiB DRAM RAM console at
`0x44400000`, with PSTORE disabled. The CM12 source uses the same address and the
same 16-u32 console header. Booting another ordinary kernel can overwrite evidence.
TWRP exposes no usable `/dev/mem`, `/dev/kmem`, `/proc/kcore`, or matching native
physical-memory interface. No generic memory node or incompatible module was added.

The isolated capture recipe is:

```sh
bash scripts/diagnostics/build-console-capture-kernel.sh
docker logs -f cm12-biscuit-build
# Only after a successful build:
python3 scripts/diagnostics/make-console-capture-boot.py
python3 scripts/diagnostics/decode-fireos6-console.py --self-test
```

The build runs detached, refuses to replace a running container, and stages only
under `workspace/diagnostics/cm12-console-capture-build/`. It reserves the original
console pages before allocator initialization, disables its own MTK RAM-console
writer, and compiles `scripts/diagnostics/fireos6-console-reader.c` into the kernel.
The proc reader permits only that fixed 64-KiB region, requires root mode 0400 and
`CAP_SYS_RAWIO`, checks valid/reserved pages and header bounds, and has no write path.
Its format/bounds self-check runs before creating the proc entry. The host decoder
also checks truncated/corrupt input and partial/wrapped circular-buffer ordering.
Neither stable ROM trees nor prebuilts are replaced.

The kernel-only build completed successfully (container exit 0). The packager reuses
the exact positive-control ramdisk and boot metadata, validates ARM64 Image magic,
the reader marker, load ranges, components and boot ID. It also preserves the
pinned control ZIP's boot-only installer byte-for-byte, replacing only `boot.img`.
The resulting image is 7,041,024 bytes, SHA-256
`3f8d0b03332ec82a4002ded5c782fb92fff8bd9159528696d2a389a06bc402ce`;
the ZIP SHA-256 is `b0d6a68be85ffc022100407b7ae751aee1f6b577b6574f3ee565011b30c761c3`.

A first attempt to avoid partition writes revealed that this amonet rejects
`fastboot boot` with `unknown command`: downloading succeeded, but Linux was never
started. This is not a failed capture-kernel boot test. The guard saw the existing
fastboot transport and took no action. The image was then installed through verified
TWRP, after another fresh backup; its full image-extent readback matched exactly.
The guarded reboot succeeded (`cm12-console-capture-flashed.*`): authenticated root
ADB on ARM64 Linux 3.18.19, with the reader's ready marker at 1.692224 seconds.
`/proc/biscuit_fireos6_console` is root-only 0400. A successful read returned exactly
65,536 bytes (SHA-256 `e4bc3c8ecef876d1f8380b5493cfc6d5319a35f175556f38e049a669228fb8a4`),
and a second full read was byte-identical. The decoded console contains 64,576
bytes, including 51 recovery-process records and ARM64 addresses, with timestamps
1.946676–274.963658 seconds. It is previous TWRP activity, not the failed ARM32
kernel and not the capture kernel's own log. The guard found the ADB transport
and did not launch a helper.

The capture kernel/reader are now a positive control. A separate preservation
control also succeeded, as described below. The minimal ramdisk has no standalone `getprop`: identify it with
`/sbin/busybox cat /default.prop`, as the tracked observer already does. An ad-hoc
host check initially used the unavailable command; correcting that check required
no image, authentication, or permissions change.

Before this phase, a fresh 16-MiB backup matching the installed `-raw-up` probe was
saved as `pre-capture-phase.boot.backup.img` (SHA-256
`852c0a50c2cee958df85b9b24e83392462aa989539b47fd79c690cfb0d281694`).
The device then entered fastboot through TWRP's `reboot-amonet`. Saved
`fastboot-pre-capture.getvar.log` confirms amonet 1.1.0, slot `a`, and download limit
`0x6c00000`. The later unsupported RAM-boot command did not write partitions; its
log is `cm12-console-capture-control.fastboot-boot.log`.

Local LK disassembly plus that published limit implies a download scratch area at
`0x45000000` with the documented amonet size adjustment (`0x45100000` under the
unmodified local formula), both above console end `0x44410000`. This is static-code
and live-metadata consistency, not a direct observation of handoff registers or a
readback of installed LK. Any fresh failed-kernel capture should avoid intervening
TWRP, and captured logs still require kernel/architecture/timestamp provenance.

### Fastboot preservation control

`cm12-console-capture-preserve.*` records a complete software-reboot → hacked
fastboot → reinstallation of the same known capture kernel → normal boot cycle.
A fresh 16-MiB boot backup matched the running collector before starting.
Afterward, boot readback matched the collector image exactly, and **all 65,536
RAM-console bytes were unchanged** (same raw SHA-256 as the initial capture).
The decoded console SHA-256 remained
`109f129f659347fdb05e25dd2cdd45d12c2f10fceda2653b04571f6790ef0509`.
This validates the collection path for this reset sequence, not every possible
watchdog or power-loss retention behavior.

Two unsupported commands were diagnosed without treating them as Linux failures:
`fastboot boot` is unknown, and `fastboot flash boot` rejects the nonexistent bare
partition. The successful boot-only command was `fastboot flash boot_a_x`, after
confirming amonet 1.1.0, current slot `a`, and a 16-MiB `boot_a_x`. No system flash
or slot change was performed. `AGENTS.md` now uses this verified example.

The installed/packaged payload was built on March 20, 2026. Its code starts after
a `0x240`-byte prefix: file-zero maps to `0x4bcfcdc0`, while the code starts at
linker address `0x4bcfd000`. Six independent string references corroborated that
mapping. Inspection of its actual flash wrapper, starting at file offset `0x524`,
shows the six-character `boot_a` prefix routed to `boot_a_x`; an explicit
`boot_a_x` name takes the same destination. The `_amonet` aliases instead bypass
that remapping and must not be used for ROM images. This is a limited binary
wrapper check, not proof of full equivalence with the newer local source checkout.

### Fresh stock ARM32 capture

`fireos6-stock-capture-1.*` repeated the unchanged stock 6.5.7.4 wrapped-kernel
probe with the validated collection path. TWRP installation had an exact full
boot readback, saved as `fireos6-stock-capture-1.installed-full.img`. A tagged
`/dev/kmsg` message identified TWRP immediately before the single test reboot.
No probe ADB appeared; the one-shot fastboot guard caught preloader at 03:12:48.
The known collector was reinstalled through confirmed hacked fastboot, booted,
and readback-verified without intervening TWRP.

The captured 65,536-byte buffer has SHA-256
`a9aeec992635195f2df6cd3c671b7d0acb8cb23d9b7677f0b050aab28e6ebe6a`.
It retains `[DEBUG-biscuit-preboot-final-fireos6-stock-capture-1]` at TWRP uptime
734.313897 s and ends in TWRP's normal ARM64 `SyS_reboot`/`arch_reset` trace at
735.073656 s. This is **not an ARM32 panic**. The ring contains 43 recovery-process
records and 29 ARM64-address matches; no identifiable new ARM32 console was found.

The reference Fire OS 6 DRAM console initializer clears the old log before
registering its console. Retaining this TWRP marker therefore narrows attention
to an early stop or failure to initialize that console; it does not prove that
no ARM32 instruction executed, nor distinguish handoff, decompression, DT setup,
and console-mapping failure on its own.

### Entry beacon preparation

`scripts/diagnostics/arm32-entry-beacon.S` contains an eight-instruction diagnostic
and a standalone ARM32 userspace self-test. The test uses anonymous **virtual**
memory, not physical memory or device nodes. The kernel variant replaces only
the eight verified initial zImage NOPs, recording marker `0xa5000000`, `r1`, `r2`
and CPSR at `0x4440fff0` (the final 16 bytes of the reserved console). It does not
use a stack or alter `r0`–`r2`, flags, the MTK wrapper, DTB or ramdisk.

Build only these diagnostic artifacts in detached Docker. Reuse the fixed
container name only after confirming it is stopped and preserving previous logs:

```sh
docker rm cm12-biscuit-build
docker run -d --name cm12-biscuit-build -v "$PWD:$PWD" -w "$PWD" \
  cm12-ubuntu14:latest bash -lc '
set -eu
out="$PWD/workspace/diagnostics/cm14-adb-boot-probe/arm32-entry-beacon"
tc="$PWD/workspace/cm14.1/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9/bin/arm-linux-androideabi"
mkdir -p "$out"
"${tc}-as" -o "$out/selftest.o" scripts/diagnostics/arm32-entry-beacon.S
"${tc}-ld" -e _start -Ttext=0x1000000 -o "$out/selftest" "$out/selftest.o"
"${tc}-objcopy" -O binary -j .text.beacon "$out/selftest.o" "$out/beacon.section.bin"
'
```

`make-adb-boot-probe.py --entry-beacon` explicitly enables the extra
`fireos6-stock-entry` and `fireos6-stock-raw-entry` outputs and asserts the exact assembled instruction bytes,
excluding the self-test's return instruction. The ordinary generator remains
unchanged by default. Run and verify the userspace self-test before installing
this diagnostic. A missing marker alone is inconclusive: later writes or
incorrect entry MMU/cache state can also hide it. Device-test results follow below.

The detached assembly build passed. The static ARM32 ELF self-test has SHA-256
`76327ce87505d617252d448f037c04e0ee2bc81e268febb0b14c1791bfca72c6`.
It ran successfully under the verified ARM64 collector, reporting both
`ARM32_ENTRY_BEACON_SELFTEST_OK` and `BEACON_SELFTEST_RC=0`; the physical console
was byte-identical before and after. This validates the instructions in userspace,
not the kernel-entry memory/cache state. The collector has no BusyBox `timeout`
applet; the test used the shell's CPU limit instead:

```sh
adb -s G090L91073533XR7 push \
  workspace/diagnostics/cm14-adb-boot-probe/arm32-entry-beacon/selftest \
  /tmp/arm32-entry-beacon-selftest &&
adb -s G090L91073533XR7 shell '
/sbin/busybox chmod 0700 /tmp/arm32-entry-beacon-selftest &&
ulimit -t 2 && /tmp/arm32-entry-beacon-selftest
rc=$?; printf "BEACON_SELFTEST_RC=%s\\n" "$rc"
'
```

Use that command only on the verified probe with `/tmp` mounted as tmpfs, and
require both success messages: legacy adbd may not propagate a remote exit code.

The generated `fireos6-stock-entry.boot.img` is 7,063,552 bytes, SHA-256
`164eda166f93576ea8fb7ec4bbbbdc031a5229eaefd183e30f1da8e3b5844e7b`;
its ZIP SHA-256 is
`67e23b2d13b5e525ffcedbc0943ff1989f99473e2470fee620071e18aa4ce04b`.
Its differences from `fireos6-stock.boot.img` are confined to the 32-byte NOP
area and boot ID. All five preceding probe image/ZIP hashes remained unchanged.

### Wrapped entry-beacon result

`fireos6-stock-entry-1.*` records a single TWRP-installed/readback-verified test,
followed by guarded hacked fastboot and the verified collector. No probe ADB
appeared. The preloader was caught at 04:24:34, without intervening recovery.
The raw console SHA-256 is
`13c6274fb1ef6de92cb857796e34f3cdae74381bbe7d6f8fd06a74ddcb93f3f3`.
The entry signature was absent; the final TWRP preboot marker and its normal
ARM64 reboot trace remained (51 recovery-process records, 46 ARM64-address
matches). Collector boot readback still matched the known image exactly.
This is a missing retained beacon, not conclusive proof that entry was never reached.

The next isolated variant, `fireos6-stock-raw-entry`, removes exactly the single
512-byte MTK header from that instrumented kernel. Its underlying kernel/DTB,
ramdisk, load addresses and cmdline are unchanged; only required size/boot-ID
metadata differs. Generation preserved all earlier image and ZIP hashes.
The new image is 7,063,552 bytes, SHA-256
`c42b933b524758fdf5d7a9908205bc4fa8c29b7489381b20319cc8b09fb6461f`;
ZIP SHA-256 is
`3e23468d5b73e41ac8f8429eebf37136b93535451bef814be820f197f7fefedd`.

### Raw entry-beacon result

`fireos6-stock-raw-entry-1.*` records a single TWRP install, exact full boot
readback and reboot. No probe ADB appeared before the observer deadline; the
guard captured preloader at 04:44:46 and sent FACTFACT. The collector was
reinstalled through verified hacked fastboot without intervening TWRP and its
boot image readback matched exactly.

The 65,536-byte raw console SHA-256 is
`bf22bf000931b5974c99e10836168417c2b5e4b5378d3e573873e48385f59ca5`.
Again, the entry signature was absent while the final TWRP preboot marker and
normal ARM64 reboot trace remained (51 recovery-process records, 46 ARM64-address
matches). The precise failing stage remains unresolved; neither wrapper removal
nor the existing userspace beacon self-test establishes a valid negative
kernel-entry observation. Further entry instrumentation needs a positive
kernel-context/physical-store control or additional handoff evidence, not a
bootloader modification based on an assumed cause.

The device was returned to the known authenticated ADB collector, not Android.
